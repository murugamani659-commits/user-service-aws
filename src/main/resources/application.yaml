server:
  port: 8080
spring:
    application:
      name: user-service
    datasource:
      url: jdbc:mysql://user-service-db.ctswou0q49po.ap-south-2.rds.amazonaws.com:3306/user_db?createDatabaseIfNotExist=true
      username: admin
      password: MohanRajaMM55
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
          format_sql: true
#  eureka:
#    client:
#      service-url:
#        defaultZone: http://localhost:8761/eureka/
#      fetch-registry: true
#      register-with-eureka: true
#    instance:
#      prefer-ip-address: true
#      instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
management:
    endpoints:
      web:
        exposure:
          include: health,info,metrics